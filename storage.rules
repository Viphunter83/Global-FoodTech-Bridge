rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read for verifying product images/docs
      allow read: if true;
      // Restrict write to authenticated users (e.g. manufacturers uploading batch data)
      allow write: if request.auth != null;
    }
  }
}
